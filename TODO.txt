Rename 'output' to resolve

Store 'least depth different' sample for reflections, and use that, rather than the first/

Investigate a 'noise removal' blur shader. Using results of edge detection as a mask between original / blurred image.
	Also - make it seperable?

Make screen quad frag shaders a util base class that uses a static geom

Consider putting depth into normal A channel - to reduce texture lookups

Experiment with different pixel formats on same framebuffer